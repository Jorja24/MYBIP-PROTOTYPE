import React from 'react';

// Per Gemini API guidelines, the API key must be sourced from environment variables.
// UI elements for entering the key are not permitted. This component is being
// disabled to comply with this requirement.
const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
